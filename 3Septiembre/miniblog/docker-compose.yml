services:
  web:
    build: .
    ports:
      - "8080:5000"
    environment:
      - DB_HOST=db
      - POSTGRES_PASSWORD=demo
    depends_on:
      - db

  db:
    image: postgres:16
    environment:
      - POSTGRES_PASSWORD=demo
    volumes:
      - datos_miniblog:/var/lib/postgresql/data

volumes:
  datos_miniblog: